name: Action custom

on:
  release:
    types: [released]
    branches: [main]


jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: ./custom-actions
        with:
          TEXT: "Hola mundo"
