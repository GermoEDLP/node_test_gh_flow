name: Action custom

on:
  release:
    types: [released]
    branches: [main]


jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Run
        run: |
             echo "Texto de ejemplo"
